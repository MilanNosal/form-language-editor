package generated;

// <editor-fold defaultstate="collapsed" desc="Imports section generated by '/src/main/resources/yajco/fle/generator/templates/imports/Imports.vsl'">
// TODO: prepare a real base
import prototyped.ConceptPanelBase;
import yajco.fle.panels.PanelAccessor;

import yajco.fle.panels.primitive.IntegerPanel;
// </editor-fold>

public class ConstraintPanel extends ConceptPanelBase implements PanelAccessor<prototyped.model.Constraint> {

    // <editor-fold defaultstate="collapsed" desc="Properties declarations">
    private final PanelAccessor<Integer> min = new IntegerPanel();
    private final PanelAccessor<Integer> max = new IntegerPanel();
    // </editor-fold>

// TODO konstruktor + pridanie ale nezabudntut nastavit label a desc miskinym panelom
    // <editor-fold defaultstate="collapsed" desc="PanelAccessor interface implementation (differs between abstract and concrete concepts)">
    @Override
    public void setValue(prototyped.model.Constraint value) {
        this.min.setValue(value.getMin());
        this.max.setValue(value.getMax());
    }

    @Override
    public prototyped.model.Constraint getValue() {
        return new prototyped.model.Constraint(
                this.min.getValue(),
                this.max.getValue()
        );
    }

    @Override
    public void reset() {
        this.min.reset();
        this.max.reset();
    }

    @Override
    public void setLabel(String label) {
        System.err.println("Setting label on ConstraintPanel is not supported.");
    }

    @Override
    public void setDescription(String description) {
        System.err.println("Setting description on ConstraintPanel is not supported.");
    }
    // </editor-fold>

    // TODO: prepare abstract concept template too
}
